# Roadmap

Here is the basic plan:

1. Run some basic probability queries, which are provably correct. Include canonical examples from the text.

2. Understand scaling bottlenecks -- i.e. is it cardinality or network depth?

3. Define continuous-to-discrete functional domain mappings. This means: provide a simple parametric method for mapping a 

3. Implement a learning algorithm, which allows for Bayesian updates to the system parameters upon the observation of new data.

4. Build the web app which renders this information in a user-friendly way.
